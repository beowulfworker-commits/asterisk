; /etc/asterisk/pjsip.conf
; Base include file (managed by install.sh)
;
; Generated by install.sh:
;   - /etc/asterisk/pjsip_transport.conf
;   - /etc/asterisk/pjsip_endpoints.conf

[global]
type=global
user_agent=VoIP-VPS
endpoint_identifier_order=auth_username,username,ip,anonymous

#include pjsip_transport.conf
#include pjsip_endpoints.conf
